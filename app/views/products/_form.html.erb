<%= form_for @product, html: {:multipart => true } do |f| %>

  <%= f.label :price %>
  <%= f.number_field :price %>

  <%= f.label :description %>
  <%= f.text_area :description, :rows => 10 %>

  <%= f.file_field :photo %>

  <%= f.submit "Submit" %>

<% end %>
